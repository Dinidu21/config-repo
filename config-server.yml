server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          timeout: 10
          refreshRate: 3600
          cloneOnStart: false

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health,info
