eureka:
  instance:
    hostname: ${EUREKA_HOSTNAME}
    prefer-ip-address: false

  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_PEER_URL}
